# Zależności interfejsu Streamlit (`streamlit run app.py`).
# Desktop (monitor_boost.py): z repo root — pip install -r requirements-desktop.txt
requests>=2.28.0
streamlit>=1.28.0
streamlit-autorefresh>=1.0.1
